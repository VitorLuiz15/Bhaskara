#include <stdio.h>
#include<stdlib.h>
 
int main() {
    int N, n1, d1, n2, d2;
    char op;

    scanf("%d", &N);

    for(int i = 1 ; i <= N; i++){
        scanf("%d / %d %c %d / %d", &n1, &d1, &op, &n2, &d2);

    }
 

    return 0;
}